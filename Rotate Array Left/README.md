# Rotate Array Left

## Problem Statement
Rotate an array to the left by k positions.

## Approach
- Use Collections.rotate() with a negative rotation value.
- Convert the array into a List using Arrays.asList().

## Time Complexity
O(n)

## Space Complexity
O(n)
